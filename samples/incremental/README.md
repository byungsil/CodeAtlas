# Incremental Regression Fixtures

These fixtures support Milestone 4 incremental correctness tests.

Each scenario has a `before/` and `after/` snapshot.

Current covered scenarios:

- `add_file`
- `delete_file`
- `edit_symbol_rename`
- `rename_move`
- `header_comment_change`
- `mass_churn`
